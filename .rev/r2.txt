Verified everything against the committed PR head (174bc47), with `main` as baseline. Note: the working tree had **uncommitted** changes to `interpreter.py` that already fix the burst-limiter bug (a `_raise_depth` rewrite) — I reviewed the committed PR state, and flagged it below since that fix isn't in the PR yet.

Full suite passes (2675) and all 104 Stately machines construct with **zero** new failures, so several fixes are genuinely clean: the atomicity rollback, deterministic exit ordering, plugin containment, combined history entry path, actor teardown leak, and sync `_enter_states`/`_exit_states` real-event forwarding (a real engine-divergence fix).

```json
{"findings":[
{"title":"Custom-id registry hijacks '#machineId' targets — nested state can shadow the machine root","severity":"CRITICAL","category":"correctness-regression","file":"src/xstate_statemachine/resolver.py","repro":"from xstate_statemachine import create_machine\nfrom xstate_statemachine.resolver import resolve_target_state\ncfg={'id':'m','initial':'a','states':{'a':{'on':{'GO':'#m.b'}},'b':{},'deep':{'initial':'x','states':{'x':{'id':'m','initial':'b','states':{'b':{}}}}}}}\nm=create_machine(cfg)\nprint(resolve_target_state('#m.b', m.states['a']).id)","actual":"m.deep.x.b (registry: {'m': 'm.deep.x'}) — on main: m.b","expected":"m.b — the machine root must win for '#<machineId>.<path>'","why_it_matters":"The new custom-id branch runs BEFORE the `segments[0] != machine.key` root check, so it takes absolute precedence. The duplicate-id guard cannot catch this because the root is never registered (`if parent is not None`), so a child declaring id equal to the machine id passes validation and silently steals every existing '#machineId.*' target. Same class of bug when a custom id equals any state key. Silent wrong-state entry, no error. Fix: check `segments[0] != machine.key` first, and register the root in `_custom_ids`."},

{"title":"Legitimate event backlog over max_iterations is silently discarded by the burst limiter","severity":"CRITICAL","category":"data-loss","file":"src/xstate_statemachine/interpreter.py","repro":"cfg={'id':'m','initial':'a','states':{'a':{'on':{'PING':{'target':'a','actions':['tick']}}}}}\n# 3000 plain sends, no action raises anything\nfor _ in range(3000): await i.send('PING')","actual":"N=1500 -> processed=1001, DROPPED=499; N=3000 -> processed=1001, DROPPED=1999","expected":"all 3000 processed; only a self-feeding `raise` chain should be broken","why_it_matters":"`burst` increments whenever the queue is non-empty after a macrostep, which is exactly what a busy external producer looks like. Any app sending >1000 events faster than they drain silently loses them — worse than the hang it replaces, because it is undetectable. NOTE: the uncommitted working-tree version already replaces this with a `_raise_depth` counter that I verified drops 0 of 3000; that rewrite is correct and must be committed into this PR."},

{"title":"from_snapshot of a stopped machine cannot be started — the error message's own advice fails","severity":"HIGH","category":"api-regression","file":"src/xstate_statemachine/interpreter.py","repro":"i=Interpreter(m); await i.start(); await i.stop()\nsnap=i.get_snapshot()      # status field == 'stopped'\nr=Interpreter.from_snapshot(snap,m)\nawait r.start()","actual":"InvalidConfigError: ...cannot be restarted. Create a new interpreter, or restore one with `Interpreter.from_snapshot(...)`. — on main this returned self","expected":"restoring a persisted snapshot and starting it should work, or the message should not recommend a path that also raises","why_it_matters":"`from_snapshot` copies `snapshot['status']` verbatim, so a snapshot taken after stop() (the normal shutdown-then-persist order) restores as 'stopped' and start() raises. The advertised recovery route is a dead end. Confirmed identically in SyncInterpreter. Persist-on-shutdown is the single most common persistence pattern; snapshotting while still running happens to work, which makes this fail only for the users who did it in the safer order."},

{"title":"Dotted-key rejection breaks configs that worked correctly on 0.5.0","severity":"HIGH","category":"breaking-change","file":"src/xstate_statemachine/models.py","repro":"# worked on main, raises on HEAD\ncreate_machine({'id':'m','initial':'v1.0','states':{'v1.0':{}}})\n# and on main this actually RAN correctly:\ni=SyncInterpreter(create_machine({'id':'m','initial':'idle','states':{'idle':{'on':{'GO':'v1.0'}},'v1.0':{}}})); i.start(); i.send('GO')  # -> {'m.v1.0'}","actual":"InvalidConfigError: State key 'v1.0' ... contains '.'","expected":"reject only the genuinely ambiguous case (a dotted key whose id collides with an existing nested path), not every dot","why_it_matters":"The stated justification is id collision between a flat 'x.y' and nested x>y — but that collision requires BOTH to exist. Version-like ('v1.0'), filename-like ('file.txt') and namespaced keys are unambiguous and demonstrably ran correctly on main (verified above). This is a hard parse-time failure on machines that previously worked, in a PyPI release. Note the check is also inconsistent: it exempts the root (`parent is not None`), and other separator-adjacent characters ('#', ':', spaces) are still accepted, so it does not actually close the ambiguity class it claims to."},

{"title":"send() drop guard omits the 'error' status, so the queue leak it fixes still occurs","severity":"MEDIUM","category":"incomplete-fix","file":"src/xstate_statemachine/interpreter.py","repro":"# machine whose invoked service raises, no onError -> status becomes 'error'\nawait i.start(); await asyncio.sleep(0.3)\nprint(i.status)  # 'error'\nfor _ in range(1000): await i.send('PING')\nprint(i._event_queue.qsize())","actual":"status='error', queue size = 1000 (unchanged from main)","expected":"0 — nothing drains the queue in 'error' either","why_it_matters":"The guard checks `status in ('stopped','done')` but `_fail()` introduces a fourth terminal status, 'error', in which the run loop is equally not draining. The unbounded-growth leak this fix targets is fully reproducible via the error path. Also an engine divergence: SyncInterpreter.send uses `!= 'running'`, which correctly covers all terminal statuses — the async guard should use the same predicate."},

{"title":"restart-after-stop raise is inconsistent: 'done' status still silently returns self","severity":"LOW","category":"inconsistency","file":"src/xstate_statemachine/sync_interpreter.py","repro":"cfg={'id':'m','initial':'a','states':{'a':{'on':{'GO':'f'}},'f':{'type':'final'}}}\ns=SyncInterpreter(create_machine(cfg)); s.start(); s.send('GO')\nprint(s.status)  # 'done'\ns.start()        # no raise","actual":"both engines: status 'done', start() returns self silently, machine stays dead","expected":"same loud failure as 'stopped' — a completed machine is equally unrevivable","why_it_matters":"The stated rationale for raising ('returning a corpse hides that every send is dropped') applies verbatim to 'done', which also drops every send. The new check only special-cases 'stopped', so the exact failure mode the fix exists to surface remains silent on the completion path. At least it is consistently wrong across both engines."}
]}
```